'use strict';

$(document).ready(initialize);

function initialize(){
  $(document).foundation();
  try {
    console.log(badkjf);

  } catch(e) {
    console.log('you just received the error: ' + e);
  }
}
